import { FC } from "react";
import MainView from "../components/main-view/MainView";

const CreateWorkoutPage: FC = () => {
  return (
    <>
      <MainView title={"Add exercises to + dynamic ending"}></MainView>
    </>
  );
};

export default CreateWorkoutPage;
