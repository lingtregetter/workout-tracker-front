import { FC } from "react";
import MainView from "../components/main-view/MainView";

const ProgramViewPage: FC = () => {
  return (
    <>
      <MainView title={"DYNAMIC PROGRAM NAME"}></MainView>
    </>
  );
};

export default ProgramViewPage;
